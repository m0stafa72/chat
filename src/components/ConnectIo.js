
import openSocket from 'socket.io-client';

   
function connect (isAuth,user_key) {
    console.log(isAuth);
    // if (isAuth) {
    //     const socket = openSocket("http://localhost:4001/");

    //     socket.emit('new user' , user_key);


    // }
}


export  { connect };